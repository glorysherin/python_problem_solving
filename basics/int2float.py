# write a program to convert floating point number into the corresponding integer
integer= int(input("Enter a integer number : "))
print("The floating point value of",str(integer),"is :",str(float(integer)))